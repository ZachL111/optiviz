# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its numerical computing focus without claiming live deployment or external usage.

## Cases

- `baseline`: `step size`, score 212, lane `ship`
- `stress`: `gradient drift`, score 210, lane `ship`
- `edge`: `residual pressure`, score 210, lane `ship`
- `recovery`: `convergence`, score 176, lane `ship`
- `stale`: `step size`, score 185, lane `ship`

## Note

A future change should add new cases before it changes the scoring rule.
